---
"@yagejs/core": minor
---

Add `Process.elapsed` — seconds accumulated from the dt the process is ticked with, so it reflects any time scaling applied by the caller. Does not advance while paused.
