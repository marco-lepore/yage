---
"@yagejs/core": patch
---

Correct three documentation examples: the `SceneManager.preload` progress bar uses `UIProgressBar.update({ value })`, the `Inspector.drive` example handles a missing entity position, and `Entity.requireKey` names the entity's `setup()` or a component's `onAdd()` as the place to call it.
