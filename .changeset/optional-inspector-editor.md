---
"@yagejs-tools/editor": patch
---

The preview installs an Inspector before the project's plugins, so selection boxes built from the collider and render facets work whether or not the harness includes `DebugPlugin`.
