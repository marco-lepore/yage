---
"@yagejs/ui-react": minor
---

React UI trees preserve keyed child order without duplicate root or container entries.

`ScrollView` now forwards its ref to the `UIScrollView` instance. `useSplitText` also reconnects its split listener when React replaces the underlying node.
