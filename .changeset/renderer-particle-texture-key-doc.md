---
"@yagejs/renderer": patch
---

`registerTexture`'s doc comment names the particle emitter's `texture` option as the key-based surface it feeds; the emitter's separate `textureKey` option is gone.
