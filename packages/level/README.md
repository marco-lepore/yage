# @yagejs/level

Level documents for the [YAGE](https://yage.dev) 2D game engine: read,
validate, prepare and instantiate authored levels. A game depends on this
package alone — the editor that writes the files is a separate development
tool.

The package has no implementation yet. It carries its two entry points, `.`
and `./document`, and nothing else.
