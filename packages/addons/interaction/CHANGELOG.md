# @yagejs-addons/interaction

## 0.3.0

### Patch Changes

- [#300](https://github.com/marco-lepore/yage/pull/300) [`08b0d06`](https://github.com/marco-lepore/yage/commit/08b0d06b63a44a51bd6f8e8308574fd41c96af59) Thanks [@marco-lepore](https://github.com/marco-lepore)! - Correct the `Interactor.onDisable` doc comment describing when its range and focus transitions reach listeners. Destroying the interactor's own entity marks it first, so the emits are dropped; destroying an ancestor deactivates it before the cascade marks it, so there they are delivered.

- Updated dependencies [[`dc42ba4`](https://github.com/marco-lepore/yage/commit/dc42ba40cd3bbd04c8ff27bf4e8721f274dde034), [`daa8214`](https://github.com/marco-lepore/yage/commit/daa821458a69d14176f5c5aebc3f4204348ddb0c), [`c105024`](https://github.com/marco-lepore/yage/commit/c105024b5402c11dc36da52b08f6ab39354da8a5), [`c8ad215`](https://github.com/marco-lepore/yage/commit/c8ad215530681caeb63484cc07b118cd977a5ba5), [`08b0d06`](https://github.com/marco-lepore/yage/commit/08b0d06b63a44a51bd6f8e8308574fd41c96af59), [`33d00e3`](https://github.com/marco-lepore/yage/commit/33d00e37801a300710cc10de0352b1aa1b1ba2f1), [`7275620`](https://github.com/marco-lepore/yage/commit/7275620756183b22de3df1009e1e07615db9b40e), [`4bab66f`](https://github.com/marco-lepore/yage/commit/4bab66f0e34a387155bbc7168b048dcac167525f), [`cfde97d`](https://github.com/marco-lepore/yage/commit/cfde97de2c94416cb5bbab26a12f9c290e6b66cf), [`9e194ec`](https://github.com/marco-lepore/yage/commit/9e194ec386a74c0f1ad5699c3c0db183aa86f1b1), [`33d00e3`](https://github.com/marco-lepore/yage/commit/33d00e37801a300710cc10de0352b1aa1b1ba2f1), [`0ca4c91`](https://github.com/marco-lepore/yage/commit/0ca4c91b46a7d147da803d0d6db54e8e1b5489ce), [`05492cb`](https://github.com/marco-lepore/yage/commit/05492cb8e27f89fe82fedd6e307afa2f90d1f68f), [`aed53f7`](https://github.com/marco-lepore/yage/commit/aed53f7f5679f824846dee3c55c0342f7f07cf98), [`ba57361`](https://github.com/marco-lepore/yage/commit/ba5736175e8b3e06157e680b4b66d10eb8d06823), [`aaf1279`](https://github.com/marco-lepore/yage/commit/aaf1279455bc655681cf15c8edc64b1407b2a823), [`8064fa6`](https://github.com/marco-lepore/yage/commit/8064fa64099feeb1d164360b668e0721a14b7bbe), [`8f11936`](https://github.com/marco-lepore/yage/commit/8f119362281bf31ab59b8b907816886922aaf18f), [`b087462`](https://github.com/marco-lepore/yage/commit/b087462ab2ae27bebb7ce274402c9e278f6d472a), [`8bb9e0b`](https://github.com/marco-lepore/yage/commit/8bb9e0b905017ac724f70fc8fe55014605563e88), [`8d7b5e3`](https://github.com/marco-lepore/yage/commit/8d7b5e3fe395898c7f4cbde0b352acc2713e6559), [`ff52a8a`](https://github.com/marco-lepore/yage/commit/ff52a8a4816b18f7de5309ab08606183db67e071)]:
  - @yagejs/core@0.11.0
  - @yagejs/input@0.11.0

## 0.2.0

### Minor Changes

- [#217](https://github.com/marco-lepore/yage/pull/217) [`87f4923`](https://github.com/marco-lepore/yage/commit/87f4923ad71f3d6096907b54c3f16d806fe57a3f) Thanks [@marco-lepore](https://github.com/marco-lepore)! - Interaction now follows entity activeness, so an entity you turn off or hand back to a pool takes its interaction state with it.
  - An `Interactable` registers while it is running and unregisters while it is not. A disabled component or a dormant entity is no longer a focus candidate for any interactor, and `interactablesIn()` does not return it. Its focus tie-break order is claimed once, so a target that goes dormant and comes back does not jump the queue.
  - An `Interactor` empties its in-range snapshot (emitting the transitions) when its entity is deactivated, the same way it already did for `enabled = false`, and `interact()` does nothing while the entity is dormant.
  - `Interactor` no longer installs its own `enabled` accessor: `Component.enabled` fires the enable hooks by itself, so the toggle keeps working through the engine's own lifecycle.

  `Interactable.isEnabled()` is unchanged — it stays the game's own gate and says nothing about whether the entity is live.

### Patch Changes

- Updated dependencies [[`34d45fd`](https://github.com/marco-lepore/yage/commit/34d45fd690d747b7d8dd36a5972ef20d21d574da), [`34d45fd`](https://github.com/marco-lepore/yage/commit/34d45fd690d747b7d8dd36a5972ef20d21d574da), [`f48983d`](https://github.com/marco-lepore/yage/commit/f48983dbb4e43c25b455ac3f96e7d8684266bbc3), [`042755b`](https://github.com/marco-lepore/yage/commit/042755b5649a90e99c8840747349255fbb3f95be), [`f1048ab`](https://github.com/marco-lepore/yage/commit/f1048ab756feee84e593609521c3a58fcfc1c1a7), [`4a5b3b6`](https://github.com/marco-lepore/yage/commit/4a5b3b639ddcbb285b6a4733b89d27bcee14c50c), [`d459026`](https://github.com/marco-lepore/yage/commit/d4590265b9aa5297fb99d20b92bb5a2f19cac0c5)]:
  - @yagejs/core@0.10.0
  - @yagejs/input@0.10.0

## 0.1.0

### Minor Changes

- [#175](https://github.com/marco-lepore/yage/pull/175) [`a11f256`](https://github.com/marco-lepore/yage/commit/a11f256f45050350d1587ed6b12496e3bda2a8fc) Thanks [@marco-lepore](https://github.com/marco-lepore)! - Add `@yagejs-addons/interaction`, a third YAGE addon: proximity-based "walk up, see a prompt, press E" interaction, headless with no bundled presenter.
  - **`Interactable`** (a `@yagejs/core` Component) marks any entity as something an `Interactor` can focus: `new Interactable({ onInteract, prompt?, radius?, priority?, enabled? })`. `prompt` and `enabled` accept either a static value or a `() => value` provider, re-resolved every frame. Self-registers into a scene-scoped registry on add and unregisters on destroy. It also exposes read-only `position`, `radius`, `priority`, `prompt`, `order`, `isEnabled()`, and its host `entity` — the per-target data a highlight outline or proximity icon reads.
  - **`Interactor`** (a `@yagejs/core` Component) is the detector: `new Interactor({ range?, action?, enabled? })`. Each `update()` ranks every in-range, enabled `Interactable` in the scene into **one snapshot** from one geometry pass: `inRange` is that snapshot and `focus` is `inRange[0]`, so the two can never disagree even when a target moves between updates. In range means `distance <= range + interactable.radius`; the order is highest `priority`, then nearest, then lowest registration order. With a non-null `action` (default `"interact"`) and a resolvable `InputManager`, the interactor fires the interact edge itself; `action: null` disables auto-input for fully manual drive. `enabled` (default true) doubles as the tracking toggle: setting it false empties the snapshot immediately (emitting the transitions) and halts tracking, input polling, and `interact()`.
  - **Interacting.** `interactor.interact()` acts on the focus; `interactor.interact(target)` acts on a chosen member of `inRange`, which is what a "which of these overlapping pickups do I grab?" selection UI confirms with. Either form does nothing unless the interactor is enabled, the target is in the current `inRange`, and the target is still live (host not destroyed, component not removed, `enabled` gate still true). To fire an interactable the interactor cannot reach — a scripted or remote trigger — call `interactable.interact()` directly; that skips every check and emits no interactor event.
  - **Events** (on the interactor's entity, bubbling to the scene). All state is assigned before any event emits, so a handler reading `focus`/`inRange` sees the new values. `InteractionFocusChangedEvent` (`{ interactable, prompt }`) fires only when the focus or its resolved prompt text changes — the prompt-render hook. `InteractionInRangeChangedEvent` (`{ inRange }`) fires when the ranked set changes: a target enters or leaves, or two swap rank. A selection UI must listen to this one, because a _non-focused_ target entering or leaving never changes the focus. `InteractionPerformedEvent` (`{ interactable }`) fires on every interaction.
  - **Scene-wide queries.** `interactablesIn(scene)` lists every live registered interactable independent of any interactor's range — for an observation skill that reveals what is interactable. It drops destroyed hosts and keeps disabled ones, since whether an ungated target is still worth revealing is the game's call. The pure `selectInteractionFocus(query, candidates)` (single winner, O(n), no sort) and `rankInteractables(query, candidates)` (the full ranked set) both encode the same priority/distance/order policy, so a custom detector gets it for nothing; both are geometry only, so filter the `enabled` gate first.
  - **Reach is validated at the edge, not per candidate.** `range` and `radius` must be non-negative: the in-range test compares squared distances, so a negative total would come back positive and match distant targets. `Interactor` and `Interactable` each dev-warn once on add when given a negative value, leaving the per-candidate test a plain comparison.
  - **`@yagejs/input` is an optional peer.** The root entry never imports a value from `@yagejs/input` — it re-declares the well-known `"inputManager"` service id locally and only imports the `InputManager` type (erased at build), so a headless game can install this addon with zero `@yagejs/input` footprint in the built output. Present, the interactor connects automatically; absent (or unregistered), it logs one dev-mode warning and falls back to manual `interact()` calls.
  - No presenters: this ships headless only. The events already carry what a bundled prompt view would need, so a `./presenters` subpath is purely additive later.

  Independently versioned (not part of the `@yagejs/*` fixed group). Ships ESM + CJS with type declarations.
