# @yagejs-addons/steering

## 0.4.0

### Patch Changes

- [#327](https://github.com/marco-lepore/yage/pull/327) [`d2adfed`](https://github.com/marco-lepore/yage/commit/d2adfedb0e5d15269fe941a3a24f23ddb0126aa4) Thanks [@marco-lepore](https://github.com/marco-lepore)! - Clarify addon composition and lifecycle contracts.
  - Skip zero-time steering updates before behavior evaluation, body access, or output. Preserve velocity and heading during freezes, including with infinite acceleration and custom apply callbacks.

- [#318](https://github.com/marco-lepore/yage/pull/318) [`33d00e3`](https://github.com/marco-lepore/yage/commit/33d00e37801a300710cc10de0352b1aa1b1ba2f1) Thanks [@marco-lepore](https://github.com/marco-lepore)! - Use the engine's global random source as `wander()`'s default. Pass a scene-scoped random callback when steering must replay with the scene.

- [#308](https://github.com/marco-lepore/yage/pull/308) [`01f3944`](https://github.com/marco-lepore/yage/commit/01f39449f8856d1ed0e3e842a6ea1173a7a49ec6) Thanks [@marco-lepore](https://github.com/marco-lepore)! - `physicsNeighbors` takes a `sensors` option, passed through to the radius query. Physics queries now skip sensor colliders by default, so a flock counts solid neighbours; pass `sensors: "include"` when the agents themselves carry sensor colliders.

- Updated dependencies [[`dc42ba4`](https://github.com/marco-lepore/yage/commit/dc42ba40cd3bbd04c8ff27bf4e8721f274dde034), [`daa8214`](https://github.com/marco-lepore/yage/commit/daa821458a69d14176f5c5aebc3f4204348ddb0c), [`daa8214`](https://github.com/marco-lepore/yage/commit/daa821458a69d14176f5c5aebc3f4204348ddb0c), [`c105024`](https://github.com/marco-lepore/yage/commit/c105024b5402c11dc36da52b08f6ab39354da8a5), [`c8ad215`](https://github.com/marco-lepore/yage/commit/c8ad215530681caeb63484cc07b118cd977a5ba5), [`08b0d06`](https://github.com/marco-lepore/yage/commit/08b0d06b63a44a51bd6f8e8308574fd41c96af59), [`33d00e3`](https://github.com/marco-lepore/yage/commit/33d00e37801a300710cc10de0352b1aa1b1ba2f1), [`7275620`](https://github.com/marco-lepore/yage/commit/7275620756183b22de3df1009e1e07615db9b40e), [`4bab66f`](https://github.com/marco-lepore/yage/commit/4bab66f0e34a387155bbc7168b048dcac167525f), [`cfde97d`](https://github.com/marco-lepore/yage/commit/cfde97de2c94416cb5bbab26a12f9c290e6b66cf), [`9e194ec`](https://github.com/marco-lepore/yage/commit/9e194ec386a74c0f1ad5699c3c0db183aa86f1b1), [`05492cb`](https://github.com/marco-lepore/yage/commit/05492cb8e27f89fe82fedd6e307afa2f90d1f68f), [`05492cb`](https://github.com/marco-lepore/yage/commit/05492cb8e27f89fe82fedd6e307afa2f90d1f68f), [`aed53f7`](https://github.com/marco-lepore/yage/commit/aed53f7f5679f824846dee3c55c0342f7f07cf98), [`01f3944`](https://github.com/marco-lepore/yage/commit/01f39449f8856d1ed0e3e842a6ea1173a7a49ec6), [`01f3944`](https://github.com/marco-lepore/yage/commit/01f39449f8856d1ed0e3e842a6ea1173a7a49ec6), [`33d00e3`](https://github.com/marco-lepore/yage/commit/33d00e37801a300710cc10de0352b1aa1b1ba2f1), [`ba57361`](https://github.com/marco-lepore/yage/commit/ba5736175e8b3e06157e680b4b66d10eb8d06823), [`0273a69`](https://github.com/marco-lepore/yage/commit/0273a69dfe675e636e1488c6c81c9072c1e64b35), [`a7eda5d`](https://github.com/marco-lepore/yage/commit/a7eda5d7cee1e163ea09362709d7ab35687f0fb6), [`aaf1279`](https://github.com/marco-lepore/yage/commit/aaf1279455bc655681cf15c8edc64b1407b2a823), [`0bc41ac`](https://github.com/marco-lepore/yage/commit/0bc41ac6c3cce2770a588d90f2662b21c458ed71), [`8064fa6`](https://github.com/marco-lepore/yage/commit/8064fa64099feeb1d164360b668e0721a14b7bbe), [`8f11936`](https://github.com/marco-lepore/yage/commit/8f119362281bf31ab59b8b907816886922aaf18f), [`b087462`](https://github.com/marco-lepore/yage/commit/b087462ab2ae27bebb7ce274402c9e278f6d472a), [`8bb9e0b`](https://github.com/marco-lepore/yage/commit/8bb9e0b905017ac724f70fc8fe55014605563e88), [`8d7b5e3`](https://github.com/marco-lepore/yage/commit/8d7b5e3fe395898c7f4cbde0b352acc2713e6559), [`8d7b5e3`](https://github.com/marco-lepore/yage/commit/8d7b5e3fe395898c7f4cbde0b352acc2713e6559), [`ff52a8a`](https://github.com/marco-lepore/yage/commit/ff52a8a4816b18f7de5309ab08606183db67e071)]:
  - @yagejs/core@0.11.0
  - @yagejs/physics@0.11.0

## 0.3.0

### Minor Changes

- [#281](https://github.com/marco-lepore/yage/pull/281) [`e5010d4`](https://github.com/marco-lepore/yage/commit/e5010d44487a2b15136603d71eb41fa6a01d86d8) Thanks [@marco-lepore](https://github.com/marco-lepore)! - Steering runs on the fixed step.
  - **Breaking:** `SteeringAgentOptions.tick` and the `SteeringAgent.update(dt)` hook are removed. `SteeringAgent` steers from `fixedUpdate` only, so Transform integration, a velocity-drive write, and an impulse-drive correction each run once per fixed step. Code that drives an agent by hand calls `fixedUpdate(dt)`. Steering turns world state into a desired velocity, which is simulation input — physics and the gameplay code that reads the result already run on that clock.
  - An agent reads the pose the physics step simulated rather than the blend the frame draws, so `arrive` radii, `followPath`'s `waypointRadius`, the flock ranges, and the `avoidColliders` ray origin all measure against simulated positions. Under a scene time scale below 1 some fixed ticks run with no physics step, and the `Transform` then holds the blend the last frame drew.
  - `faceHeading` writes the Transform rotation, which a dynamic body's simulated rotation owns. Pass `syncRotation: false` on the `RigidBodyComponent` to give the agent rotation.
  - A bodyless agent's drawn position changes once per fixed step, and nothing in the engine interpolates a `Transform` that no rigid body drives. One way to draw that motion on the frame clock is to record the commanded velocity in `apply` and integrate it in your own `update`.
  - `SteeringApplyContext.dt` carries fixed-step seconds. `stop()` still passes `dt: 0`.

## 0.2.1

### Patch Changes

- [#237](https://github.com/marco-lepore/yage/pull/237) [`efb4a88`](https://github.com/marco-lepore/yage/commit/efb4a882997bb774aed0a7befd9847a1fccad168) Thanks [@marco-lepore](https://github.com/marco-lepore)! - Steer kinematic bodies correctly.
  - `PhysicsSteeringAgent` now branches on the sibling body's type. On a kinematic body it integrates the `Transform` in `fixedUpdate` — kinematic bodies ignore `setVelocity`/`applyImpulse`, so previously the agent silently never moved them. Passing `drive` with a kinematic body throws, and mounting on a static body throws.
  - `PhysicsSteeringAgent` reads the body's type when added: add `RigidBodyComponent` before the agent, or the add throws (previously the body was resolved lazily at the first tick, so either order worked).
  - `SteeringAgent` gains `tick: "update" | "fixedUpdate"` (default `"update"`), choosing which hook steers.
  - Docs: the previous recommendation of a "kinematic velocity-based" body is corrected — the engine has no such body type, and velocity drive requires a dynamic body.

## 0.2.0

### Patch Changes

- Updated dependencies [[`34d45fd`](https://github.com/marco-lepore/yage/commit/34d45fd690d747b7d8dd36a5972ef20d21d574da), [`34d45fd`](https://github.com/marco-lepore/yage/commit/34d45fd690d747b7d8dd36a5972ef20d21d574da), [`6fc90a5`](https://github.com/marco-lepore/yage/commit/6fc90a5635395e18c6f466d36e2477f8264ddbe9), [`f48983d`](https://github.com/marco-lepore/yage/commit/f48983dbb4e43c25b455ac3f96e7d8684266bbc3), [`042755b`](https://github.com/marco-lepore/yage/commit/042755b5649a90e99c8840747349255fbb3f95be), [`042755b`](https://github.com/marco-lepore/yage/commit/042755b5649a90e99c8840747349255fbb3f95be), [`f1048ab`](https://github.com/marco-lepore/yage/commit/f1048ab756feee84e593609521c3a58fcfc1c1a7), [`4a5b3b6`](https://github.com/marco-lepore/yage/commit/4a5b3b639ddcbb285b6a4733b89d27bcee14c50c), [`4a5b3b6`](https://github.com/marco-lepore/yage/commit/4a5b3b639ddcbb285b6a4733b89d27bcee14c50c), [`d459026`](https://github.com/marco-lepore/yage/commit/d4590265b9aa5297fb99d20b92bb5a2f19cac0c5), [`6fc90a5`](https://github.com/marco-lepore/yage/commit/6fc90a5635395e18c6f466d36e2477f8264ddbe9)]:
  - @yagejs/core@0.10.0
  - @yagejs/physics@0.10.0

## 0.1.0

### Minor Changes

- [#174](https://github.com/marco-lepore/yage/pull/174) [`9ee8b30`](https://github.com/marco-lepore/yage/commit/9ee8b303555466963fb0c79d39730efff0858ea6) Thanks [@marco-lepore](https://github.com/marco-lepore)! - Add `@yagejs-addons/steering`, a steering (movement AI) addon: seek, flee, arrive, wander, pursue/evade, obstacle avoidance, flocking, path-following, and containment, blended into a desired velocity. The root entry is pure `@yagejs/core`; `@yagejs-addons/steering/physics` adds a `RigidBodyComponent`-detecting agent behind an optional `@yagejs/physics` peer.
  - **Headless model** (root entry `.`): `Steering` arbitrates behaviors by `priority` tier (highest first; the first tier whose weighted sum is non-zero wins outright and lower tiers are not evaluated that frame) and blends within a tier by `weight` (default 1), clamping the result to `agent.maxSpeed`. Everything on the default priority 0 is a plain weighted sum; `avoidObstacles(rocks, { priority: 1 })` makes avoidance override seek instead of out-voting it. Twelve behavior factories — `seek`, `flee` (with an optional flee-only-if-near `radius`), `arrive` (linear speed ramp inside `slowRadius`, settles inside `arriveRadius`, fires `onArrive`/`onDepart` once per crossing), `wander` (a slowly-turning circle ahead of the current heading, with an injectable `random` for deterministic tests), `pursue`/`evade` (lead a moving target by up to `maxPrediction` seconds, collapsing to plain seek/flee for a stationary one), `avoidObstacles` (look-ahead ray, steers around the closest threat in path), `separation`/`alignment`/`cohesion` (the three boid rules, game-supplied neighbor lists), `followPath` (waypoints in order with a `waypointRadius` advance; `loop` patrols forever, otherwise the final waypoint gets arrive semantics; progress is readable via the returned behavior's `waypointIndex` and restorable via `startAt` — an index or `"nearest"` — and it takes a plain `Vec2Like[]`, so `@yagejs/pathfinding`'s `Path.waypoints` feeds it with no dependency), and `contain` (steers back inside a `{ x, y, width, height }` rect before the look-ahead point crosses an edge). Targets, obstacles, and neighbors all accept either a static value or a `() => value` provider, resolved live each tick.
  - **`SteeringAgent`** (a `@yagejs/core` Component, the L2a host): computes a desired velocity each `update(dt)` and applies it — kinematic Transform integration by default, or a structural `body` (`VelocityBody` `{ setVelocity, getVelocity }` / `ImpulseBody` `{ applyImpulse, getVelocity, getMass }`, both satisfied by `RigidBodyComponent` without an import). With a `body`, behaviors and the `maxAcceleration` ramp read the body's **actual** velocity, so collisions, wall pins, and knockback feed back into steering. `drive: "velocity"` (default) writes the commanded velocity; `drive: "impulse"` applies the capped correction (`clamp(desired − actual, maxAcceleration·dt) × mass`) through `applyImpulse`, so external impulses compose — a dynamic agent pushes and is pushed. `maxAcceleration` defaults to `4 × maxSpeed` — the acceleration cap doubles as the low-pass that keeps steering smooth when the blend flips direction between frames (`Infinity` opts into an instant snap). `stop()` zeroes the model and pushes a zero through the output (`setVelocity(0)`, a counter-impulse, or `apply(ZERO)`). `faceHeading` rotates the Transform to the travel direction (skipping sub-1 px/s jitter). Live escape hatches: `agent.steering`, `agent.velocity`, `agent.maxSpeed`, `agent.stop()`, `enabled`.
  - **`PhysicsSteeringAgent`** (`/physics` entry): resolves the `RigidBodyComponent` sibling itself and defaults to impulse drive — mount next to a body and collider, nothing to wire. Query it back with `entity.get(PhysicsSteeringAgent)` (components are keyed by exact class).
  - **Physics-backed discovery** (`/physics` entry): `avoidColliders(world, { lookAhead, whiskerAngle, whiskerLength })` raycasts the real world along the heading (center ray + two whiskers) and steers away from the closest hit — no obstacle list, tilemap walls and crates count, the agent's own collider is excluded. `physicsNeighbors(world, { radius, filterGroups })` is a `NeighborsSource` over `PhysicsWorld.queryRadius` for flocking against whatever is physically nearby. Target/obstacle/neighbor providers receive the agent's state (`(agent) => value`), which carries the hosting entity for self-exclusion.
  - **No presenters, no game-defined ID type parameter, no entity-event stream** — the discrete consequences (arrival) surface via `onArrive`/`onDepart` callbacks, and a game wires its own event in one line if it wants one.

  Independently versioned (not part of the `@yagejs/*` fixed group), first publish `0.1.0`. Peers: `@yagejs/core` (required), `@yagejs/physics` (optional, `/physics` entry only).
